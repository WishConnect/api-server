-- 장학금 태그 기능 철회
--
-- 2026-08-16 에 scholarship_tag 를 추가하고 동기화에서 적재까지 했으나(57건),
-- PM 결정으로 태그 기능을 쓰지 않기로 했다. 응답의 tags 필드는 예전처럼 빈 배열로 돌아간다.
--
-- ⚠️ 이 SQL 은 **배포 후에** 적용할 것.
--    먼저 적용하면 아직 옛 코드가 도는 서버가 없는 테이블을 조회해 500 이 난다.
--    validate 는 엔티티에 없는 여분 테이블을 문제 삼지 않으므로, 배포 후 정리하는 순서가 안전하다.

DROP TABLE IF EXISTS scholarship_tag;
